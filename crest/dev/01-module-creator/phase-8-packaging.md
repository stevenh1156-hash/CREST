# phase-8-packaging
Status: not started.

Wrap Crest-Boot + watcher + scripts into a single .exe / installer. Use ps2exe or a bundler. User double-clicks to install, gets desktop shortcut, no PowerShell knowledge needed.
