# tier-2-standalone
Status: not started.

Build the productized version: standalone tray app + Claude API backend. No Claude desktop required. Watches FS, calls Claude API, displays results in own UI. Foundation for monetization.
