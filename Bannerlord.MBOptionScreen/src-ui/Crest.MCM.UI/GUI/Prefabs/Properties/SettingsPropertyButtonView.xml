<Prefab>
  <Window>
    <ButtonWidget SuggestedWidth="251" SuggestedHeight="64" VerticalAlignment="Center" HorizontalAlignment="Center"
                  IsEnabled="@IsEnabled" IsVisible="@IsButton" DoNotPassEventsToChildren="true" Brush="Crest.MCM.Cancel.Button" Command.Click="OnValueClick">
      <Children>
        <TextWidget WidthSizePolicy="StretchToParent" HeightSizePolicy="StretchToParent" VerticalAlignment="Center" HorizontalAlignment="Center"
                    IsEnabled="@IsEnabled" IsVisible="@IsButton" Text="@ButtonContent" Brush="Crest.MCM.Button.Text" />
      </Children>
    </ButtonWidget>
  </Window>
</Prefab>