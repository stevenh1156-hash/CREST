// Phase T (CREST Cheats sub-module) was removed at the user's request.
// File kept rather than deleted because the csproj uses a Compile glob and
// removing it would require an ItemGroup edit. This stub compiles to nothing
// and the entrypoint that previously called CrestCheats.Apply() in
// SubModule.OnSubModuleLoad has been removed.
